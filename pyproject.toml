[project]
name = "labo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fabric > 1, < 3",
    "pytest",
    "pyyaml",
    "tabulate",
    "ansible",
    "ansible-lint",
    "requests",
]

[tool.black]
line-length = 120
