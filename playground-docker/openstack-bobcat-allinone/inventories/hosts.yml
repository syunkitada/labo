workstation:
  hosts:
    localhost:

mysql:
  hosts:
    localhost:

memcached:
  hosts:
    localhost:

rabbitmq:
  hosts:
    localhost:

keystone:
  hosts:
    localhost:

glance:
  hosts:
    localhost:

placement:
  hosts:
    localhost:

nova:
  hosts:
    localhost:

neutron:
  hosts:
    localhost:

nova_compute:
  hosts:
    localhost:

neutron_openvswitch_agent:
  hosts:
    localhost:
